# 📘 Полная инструкция по установке и настройке

## Шаг 0: Установка Node.js (если еще не установлен)

### Вариант А: Через официальный сайт (рекомендую)

1. Открой https://nodejs.org/
2. Скачай версию **LTS** (Long Term Support) - это стабильная версия
3. Установи скачанный файл (двойной клик, следуй инструкциям)
4. Перезапусти терминал или открой новый

### Вариант Б: Через Homebrew (если установлен)

```bash
brew install node
```

### Проверка установки

Открой новый терминал и выполни:
```bash
node --version
npm --version
```

Должны показаться версии (например, v20.x.x и 10.x.x)

---

## Шаг 1: Установка зависимостей проекта

После установки Node.js выполни:

```bash
cd ~/remonteka
npm install
```

Это займет 2-5 минут. Дождись завершения.

---

## Шаг 2: Настройка базы данных

### Вариант А: Supabase (бесплатно, самый простой)

1. **Зарегистрируйся:**
   - Открой https://supabase.com
   - Нажми "Start your project"
   - Войди через GitHub (проще всего)

2. **Создай проект:**
   - Нажми "New Project"
   - Название: `remonteka` (или любое другое)
   - Пароль для базы данных: придумай и **сохрани его!**
   - Регион: выбери ближайший (например, Europe West)
   - Нажми "Create new project"
   - Подожди 2-3 минуты пока создается проект

3. **Получи Connection String:**
   - После создания проекта перейди в Settings (шестеренка слева)
   - Выбери "Database"
   - Прокрути вниз до "Connection string"
   - Выбери "URI" (не "Session mode")
   - Скопируй строку, она выглядит так:
     ```
     postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres
     ```

4. **Создай файл .env:**
   - Открой папку `~/remonteka` в Finder
   - Создай новый файл с именем `.env` (с точкой в начале!)
   - Открой его в текстовом редакторе
   - Вставь следующее (замени `[YOUR-PASSWORD]` на свой пароль из Supabase):
     ```
     DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres"
     NEXTAUTH_SECRET="сгенерируй-случайную-длинную-строку-здесь-минимум-32-символа"
     NEXTAUTH_URL="http://localhost:3000"
     ```
   - Сохрани файл

**Как сгенерировать NEXTAUTH_SECRET:**
- Открой https://generate-secret.vercel.app/32
- Скопируй сгенерированную строку
- Вставь в `.env` файл

### Вариант Б: Neon (бесплатно, альтернатива)

1. Открой https://neon.tech
2. Sign up через GitHub
3. Create project
4. Скопируй Connection String
5. Вставь в `.env` файл как `DATABASE_URL`

---

## Шаг 3: Создание миграций базы данных

После настройки `.env` файла выполни:

```bash
cd ~/remonteka
npm run db:generate
```

Это создаст Prisma Client.

Затем:

```bash
npm run db:migrate
```

При первом запуске Prisma спросит имя миграции - введи:
```
init
```

И нажми Enter.

Это создаст все таблицы в базе данных.

---

## Шаг 4: Запуск проекта

```bash
npm run dev
```

Дождись сообщения:
```
✓ Ready in X seconds
○ Local: http://localhost:3000
```

Открой браузер и перейди на http://localhost:3000

---

## Шаг 5: Развертывание на Vercel (опционально)

### Что такое Vercel?
Vercel - это платформа, которая автоматически задеплоит твой проект в интернет. Каждый раз когда ты делаешь push на GitHub, проект автоматически обновится.

### Как подключить:

1. **Зарегистрируйся:**
   - Открой https://vercel.com
   - Нажми "Sign Up"
   - Выбери "Continue with GitHub"
   - Разреши доступ к репозиториям

2. **Импортируй проект:**
   - Нажми "Add New..." → "Project"
   - Найди репозиторий `remonteka`
   - Нажми "Import"

3. **Настрой переменные окружения:**
   - В разделе "Environment Variables" добавь:
     - `DATABASE_URL` - тот же что в `.env`
     - `NEXTAUTH_SECRET` - тот же что в `.env`
     - `NEXTAUTH_URL` - URL который даст Vercel (например, `https://remonteka.vercel.app`)

4. **Deploy:**
   - Нажми "Deploy"
   - Подожди 2-3 минуты
   - Готово! Твой сайт будет доступен по адресу типа `https://remonteka-xxxxx.vercel.app`

---

## ❓ Частые проблемы

### "npm: command not found"
→ Node.js не установлен. Выполни Шаг 0.

### "Error: Can't reach database server"
→ Проверь `.env` файл - правильно ли скопирован DATABASE_URL из Supabase/Neon

### "Migration failed"
→ Убедись что база данных создана и пароль правильный

### Порт 3000 занят
→ Измени порт: `PORT=3001 npm run dev`

---

## ✅ Чек-лист готовности

- [ ] Node.js установлен (`node --version` работает)
- [ ] Зависимости установлены (`npm install` выполнен)
- [ ] База данных создана (Supabase или Neon)
- [ ] Файл `.env` создан с правильными данными
- [ ] Миграции выполнены (`npm run db:migrate`)
- [ ] Проект запускается (`npm run dev`)
- [ ] Сайт открывается в браузере

---

## 🆘 Нужна помощь?

Если что-то не работает:
1. Проверь все шаги выше
2. Убедись что все команды выполняются в папке `~/remonteka`
3. Проверь что `.env` файл создан и содержит правильные данные
4. Перезапусти терминал после установки Node.js
